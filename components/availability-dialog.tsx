'use client';
import { CalendarDays, X } from 'lucide-react';
import type { HotelRoomType } from '@/lib/hotel-masters';
export function AvailabilityDialog({ roomTypes, onClose }: { roomTypes: HotelRoomType[]; onClose: () => void }) { const start = new Date(); const days = Array.from({length:8},(_,i)=>{const d=new Date(start);d.setDate(d.getDate()+i);return d}); return <div className="billing-instruction-overlay" role="dialog" aria-modal="true" aria-label="Availability"><div className="availability-card"><div className="availability-head"><strong>Availability</strong><span><CalendarDays size={18}/> {start.toLocaleDateString('en-GB',{day:'2-digit',month:'short',year:'numeric'})}</span><button onClick={onClose} aria-label="Close"><X size={18}/></button></div><div className="availability-scroll"><table><thead><tr><th>Room Type</th>{days.map(d=><th key={d.toISOString()}>{d.toLocaleDateString('en-GB',{weekday:'short',day:'2-digit',month:'2-digit'})}</th>)}</tr></thead><tbody>{roomTypes.filter(x=>x.active).map((room)=> <tr key={room.code}><th>{room.code}</th>{days.map(d=><td key={d.toISOString()}>{room.totalRoom}</td>)}</tr>)}</tbody></table></div><div className="availability-actions"><button className="primary-button" onClick={onClose}>Close</button></div></div></div>; }
